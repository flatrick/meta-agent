# Known Unknowns

`PKB/` is temporary staging only.
Promote resolved items into canonical `docs/` updates and keep unresolved items here until closure.

## Staging Metadata (Required)

- `staging_status`: `staging`
- `staged_at_utc`: `{{ generated_at_utc }}`
- `last_reviewed_at_utc`: `{{ generated_at_utc }}`
- `promotion_target_path`: `docs/architecture/internal/3x/31/index.md`
- `not_promoted_reason`: `Unknowns are intentionally pending until validated and incorporated into canonical architecture records.`

Track unresolved questions discovered during exploration.

| Area | Question | Why it matters | Next action |
| --- | --- | --- | --- |
| TODO | TODO | TODO | TODO |
